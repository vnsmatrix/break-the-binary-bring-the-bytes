//create a var hasSigned, if submit before signin, false
//var c = document.querySelector('CANVAS');
//c.toDataURL()
//image is after the comma
// mouse down on canvas, record x/y postition of mouse
// start list for mouse move
// draw line from prev mouse x/y to new x/y
//reset var

data url when mouse up

post route
receive form, insert in db
if work: send cookie, redirect to ty page (do not render)
/thankyou
if error: render form again w error

/petition
in /thankyou, link to /signers
/signers

petition: get + post
query: insert, select for signers page

templates: petition, thank you, signers
js: DOM events handlins for canvas + handles puttin input field of canvas
    
script.js only in petition page!

· select count(*) from petition;


getcontext
getElementById('canvas')
